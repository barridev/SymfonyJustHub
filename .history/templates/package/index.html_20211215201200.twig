{% extends 'base.html.twig' %}

{% block title %}Liste des colis{% endblock %}

{% block body %}
    <table class="table bordered">
        <tr>
            <td>#</td>
            <td>Client</td>
            <td>Expéditeur</td>
            <td>Status</td>
            <td>Point relais</td>
            <td>Casier</td>
            <td>Poids</td>

        </tr>

        {% for book in books %}
        <tr>
                <td>{{ book.id }}</td>
                <td>{{ book.name }}</td>
                <td>{{ book.description }}</td>
                <td>{{ book.price }}</td>
                <td>
                    <a href="{{ path('book_show', {'id': book.id}) }}">Voir</a>
                    <a href="{{ path('book_edit', {'id': book.id}) }}">Editer</a>
                    <a href="{{ path('book_delete', {'id': book.id, 'token': csrf_token('delete_book')}) }}">Supprimer</a>
                </td>
            </tr>
        {% endfor %}
    </table>
{% endblock %}
